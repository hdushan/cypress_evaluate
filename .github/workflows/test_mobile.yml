name: Test Myamaysim Mobile

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Test the online mobile funnel
      uses: actions/checkout@v1

    - run: make testMobile
      env:
        ENV: production

    - uses: actions/upload-artifact@v1
      with:
        name: Screenshots and Videos
        path: tests/results/